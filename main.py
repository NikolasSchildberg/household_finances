"""Runs other modules in folder, plotting data from year budget.
Build tables (and export to csv) like the models provided, in order
to explore your household budget, too."""

import categories
import yearly_summary
import month_balance_tracker
